{% extends '::backend.html.twig' %}

{% block id 'ciudad' %}
{% block title %}Añadir una nueva ciudad{% endblock %}

{% block article %}
<h1>{{ block('title') }}</h1>

{{ form(form, { 'action': path('backend_ciudad_create') }) }}

<ul class="record_actions">
    <li>
        <a href="{{ path('backend_ciudad') }}">
            Volver al listado
        </a>
    </li>
</ul>
{% endblock %}
